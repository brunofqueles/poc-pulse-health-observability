bundle:
  name: poc-pulse-health-observability

include:
  - resources/*.yml

targets:
  dev:
    mode: production
    default: true
    workspace:
      host: https://dbc-7da36775-5ddf.cloud.databricks.com
      root_path: /Workspace/Users/bruno.quelestech@outlook.com/.bundle/${bundle.name}/${bundle.target}